---
title: Auto-fetch Agent Mock Tools
description: "Discover the agent's tools (from a managed provider or a customer-hosted MCP server) and generate mock input/output data for each. ⚠ Overwrites empty tool entries; existing entries with mock data are kept. For provider=\"custom\", supply `mcp_server_url`. Returns a `progress_id`; poll `auto-fetch-tools-progress/` for completion. This does NOT enable mock mode — use `enable-mock-tools/` for that."
openapi: post /test_framework/v2/aiagents/{id}/auto-fetch-tools/
slug: auto-fetch-agent-mock-tools
---