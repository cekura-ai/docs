---
title: Auto-fetch Agent Mock Tools
description: "Discover the agent's tools from a managed provider (Retell, VAPI, ElevenLabs) or a customer-hosted MCP server, and generate mock input/output data for each. Existing tools that already have mock data are kept. For provider=\"custom\", supply `mcp_server_url`. Returns a `progress_id`; poll the progress endpoint for completion. This does not enable mock mode."
openapi: post /test_framework/v2/aiagents/{id}/auto-fetch-tools/
slug: auto-fetch-agent-mock-tools
---