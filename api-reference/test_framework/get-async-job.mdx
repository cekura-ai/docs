---
title: Wait for a Background Job
description: "Check on a background job using the `progress_id` returned by the endpoint that started it. Waits up to `wait` seconds for the job to finish, then returns its current state; while `status` is `in_progress`, call again with the same id."
openapi: get /test_framework/v1/async-jobs/{job_id}/
slug: get-async-job
---