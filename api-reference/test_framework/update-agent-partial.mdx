---
title: Update Agent (Partial)
description: "Partially update an AI agent. Same field shape as create — see `aiagents_create` for details and per-provider examples."
openapi: patch /test_framework/v2/aiagents/{id}/
slug: update-agent-partial
---

<Note>
  The `provider` and `telephony` blocks accept the same fields as [Create Agent](/api-reference/test_framework/create-agent) — see that page for per-provider field tables and examples, including `auto_dial_outbound`, `auto_import_calls`, `auto_sync_prompt`, and `send_post_conversation_metadata`.
</Note>
