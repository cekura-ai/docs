---
title: Create Agent
openapi: post /test_framework/v1/aiagents/
slug: create-agent
---

## Request Body

<Expandable title="Basic Settings" defaultOpen={true}>
  <ParamField body="organization" type="integer" required>
    The organization this agent belongs to.
  </ParamField>

  <ParamField body="project" type="integer">
    The project this agent belongs to.
  </ParamField>

  <ParamField body="agent_name" type="string" required>
    A descriptive name for your agent. Example: `"Customer Service Assistant"`.
  </ParamField>

  <ParamField body="description" type="string">
    A detailed description of what this agent does and how it should interact.
  </ParamField>

  <ParamField body="language" type="string" default="en">
    The primary language this agent uses for communication. Options: `ar`, `bg`, `zh`, `cs`, `da`, `nl`, `en`, `fi`, `fr`, `de`, `el`, `hi`, `he`, `hu`, `id`, `it`, `ja`, `ko`, `ms`, `multi`, `no`, `pl`, `pt`, `ro`, `ru`, `sk`, `es`, `sv`, `tr`, `ta`, `uk`, `vi`.
  </ParamField>
</Expandable>

<Expandable title="Voice Integration Settings" defaultOpen={true}>
  <ParamField body="contact_number" type="string">
    Phone number assigned to this agent for calls (must start with + followed by digits). Example: `"+1234567890"`.
  </ParamField>

  <ParamField body="inbound" type="boolean">
    Whether this agent handles inbound calls. If `true`, our agent will call you. If `false`, your agent will have to call our agent.
  </ParamField>

  <ParamField body="assistant_provider" type="string" default="self_hosted">
    Service provider for the AI assistant functionality. Options: `vapi`, `retell`, `elevenlabs`, `livekit`, `pipecat`, `bland`, `synthflow`, `koreai`, `agentforce`, `trillet`, `custom`, `self_hosted`.
  </ParamField>

  <ParamField body="transcript_provider" type="string">
    Service provider used for transcription services.
  </ParamField>

  <Tabs>
    <Tab title="VAPI">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the VAPI assistant.
      </ParamField>

      <ParamField body="vapi_api_key" type="string">
        API key for VAPI provider authentication.
      </ParamField>
    </Tab>

    <Tab title="Retell">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Retell assistant.
      </ParamField>

      <ParamField body="retell_api_key" type="string">
        API key for Retell provider authentication.
      </ParamField>
    </Tab>

    <Tab title="ElevenLabs">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the ElevenLabs assistant.
      </ParamField>

      <ParamField body="elevenlabs_api_key" type="string">
        API key for ElevenLabs provider authentication.
      </ParamField>
    </Tab>

    <Tab title="LiveKit">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the LiveKit assistant.
      </ParamField>

      <ParamField body="livekit_api_key" type="string">
        API key for LiveKit provider authentication.
      </ParamField>

      <ParamField body="livekit_data" type="string">
        Additional configuration data for LiveKit.
      </ParamField>
    </Tab>

    <Tab title="Pipecat">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Pipecat assistant.
      </ParamField>

      <ParamField body="pipecat_api_key" type="string">
        API key for Pipecat provider authentication.
      </ParamField>

      <ParamField body="pipecat_data" type="string">
        Additional configuration data for Pipecat.
      </ParamField>
    </Tab>

    <Tab title="Bland">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Bland assistant.
      </ParamField>

      <ParamField body="bland_api_key" type="string">
        API key for Bland provider authentication.
      </ParamField>
    </Tab>

    <Tab title="Synthflow">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Synthflow assistant.
      </ParamField>

      <ParamField body="synthflow_api_key" type="string">
        API key for Synthflow provider authentication.
      </ParamField>
    </Tab>

    <Tab title="Kore.ai">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Kore.ai assistant.
      </ParamField>

      <ParamField body="koreai_client_secret" type="string">
        Client secret for Kore.ai authentication.
      </ParamField>

      <ParamField body="koreai_data" type="string">
        Additional configuration data for Kore.ai.
      </ParamField>
    </Tab>

    <Tab title="Agentforce">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Agentforce assistant.
      </ParamField>

      <ParamField body="agentforce_client_secret" type="string">
        Client secret for Agentforce authentication.
      </ParamField>

      <ParamField body="agentforce_data" type="string">
        Additional configuration data for Agentforce.
      </ParamField>
    </Tab>

    <Tab title="Trillet">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the Trillet assistant.
      </ParamField>

      <ParamField body="trillet_api_key" type="string">
        API key for Trillet provider authentication.
      </ParamField>

      <ParamField body="trillet_data" type="string">
        Additional configuration data for Trillet.
      </ParamField>
    </Tab>

    <Tab title="Custom">
      <ParamField body="assistant_id" type="string">
        Unique identifier for the custom assistant.
      </ParamField>

      <ParamField body="custom_provider_api_key" type="string">
        API key for custom provider authentication.
      </ParamField>

      <ParamField body="custom_provider_data" type="string">
        Additional configuration data for custom provider.
      </ParamField>
    </Tab>
  </Tabs>
</Expandable>

<Expandable title="Chat Integration Settings">
  <ParamField body="chat_assistant_id" type="string">
    Unique identifier for the chat assistant.
  </ParamField>
</Expandable>

<Expandable title="LLM Settings">
  <ParamField body="llm_system_prompt" type="string">
    System prompt that defines the agent's behavior and personality. Example: `"You are a helpful customer service agent."`.
  </ParamField>

  <ParamField body="llm_model" type="string" default="gpt-4o">
    The LLM model to use for simulating user responses. Options: `gpt-4o`, `gpt-4o-mini`, `gpt-4.1`, `gpt-4.1-mini`, `claude-sonnet-4-5-20250929`.
  </ParamField>

  <ParamField body="llm_temperature" type="number" default="0.0">
    Controls randomness in responses (0.0-2.0, lower = more focused). Example: `0.5`.
  </ParamField>

  <ParamField body="llm_max_tokens" type="integer" default="4096">
    Maximum length of simulated responses in tokens. Example: `4096`.
  </ParamField>
</Expandable>

<Expandable title="WebSocket Settings">
  <ParamField body="websocket_url" type="string">
    WebSocket endpoint URL for real-time communication. Example: `"wss://api.example.com/ws"`.
  </ParamField>

  <ParamField body="websocket_headers" type="object">
    Headers to be sent to the websocket server.
  </ParamField>
</Expandable>

<Expandable title="Additional Settings">
  <ParamField body="predefined_metrics" type="array">
    List of predefined metric IDs to associate with this agent.
  </ParamField>

  <ParamField body="knowledge_base_files" type="string">
    Files containing knowledge base content for the agent.
  </ParamField>

  <ParamField body="enabled_personalities" type="array">
    List of personality IDs that can be used with this agent.
  </ParamField>

  <ParamField body="outbound_auto_call" type="boolean">
    Whether to automatically initiate outbound calls.
  </ParamField>

  <ParamField body="outbound_numbers" type="array">
    List of phone numbers for outbound calls.
  </ParamField>
</Expandable>
