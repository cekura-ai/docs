---
title: Disable Agent Mock Tools
description: "Restore the agent's original (non-mock) tool configuration on the provider. Mock data is preserved so mock mode can be re-enabled later. Returns a `progress_id`; poll the progress endpoint for completion."
openapi: post /test_framework/v2/aiagents/{id}/disable-mock-tools/
slug: disable-agent-mock-tools
---