---
title: Update Scenario Condition Audio
description: "Replace this clip's audio, keeping its id. Every action referencing it plays the new recording; no action text changes. The clip is transcoded and re-transcribed, so runs are blocked again until that finishes."
openapi: put /test_framework/v1/scenarios/{id}/condition-audio/{audio_id}/
slug: update-scenario-condition-audio
---