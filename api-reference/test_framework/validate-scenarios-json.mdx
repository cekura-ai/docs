---
title: Validate Scenarios JSON
description: "Check a spec against the agent it targets: metric slugs, personalities, test profiles, channel support and every tag. Returns the plan a run would follow, with case count and estimated cost. Creates nothing and places no calls. Use this before committing a suite or opening a pull request; use `scenarios_run_json` to execute one."
openapi: post /test_framework/v1/scenarios/validate_scenarios_json/
slug: validate-scenarios-json
---