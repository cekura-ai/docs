---
title: Create API Key
description: "Create a new API key. When using API key authentication, only project-level API keys can be created."
openapi: post /user/api/v1/user-api-key/
slug: create-api-key
---

<Warning>
**Save your API key immediately.** The full API key is only displayed once when you create it. After creation, you will only see a masked version (e.g., `abc12345****`). If you lose your key, you must generate a new one—it cannot be recovered.
</Warning>
