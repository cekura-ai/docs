---
title: Update Scenario Condition Audio
description: "Replace this clip's audio. Every action referencing it plays the new recording. Pass `name` to rename the clip at the same time — its `<audio>` tags are repointed for you, and the response carries `previous_audio_id` and the rewritten `actions`. The clip is transcoded and re-transcribed, so runs are blocked again until that finishes."
openapi: put /test_framework/v1/scenarios/{id}/condition-audio/{audio_id}/
slug: update-scenario-condition-audio
---